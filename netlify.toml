[build]
  publish = "dist/client"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"

# Redirect unknown routes to index.html for SPA routing
# Netlify automatically serves existing files first
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200